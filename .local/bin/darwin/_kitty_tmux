#!/bin/bash -l

export XDG_CONFIG_HOME=~/.config

_tmux() {
    /opt/homebrew/bin/tmux \
        -L "__kitty" -f $XDG_CONFIG_HOME/tmux/kitty.conf \
        new-session -t "__kitty_${KITTY_PID}_${KITTY_WINDOW_ID}" \
        "$@"
}

_tmux "$@"
